import { Tooltip } from './Tooltip';
import type { TooltipProps, Placement } from './types';
import { useTooltip } from './useTooltip';
import type {
    TooltipConfig,
    TooltipRenderProps,
    TooltipTriggerProps,
} from './useTooltip';

export default Tooltip;
export { Tooltip, useTooltip };
export type {
    TooltipProps,
    Placement,
    TooltipConfig,
    TooltipRenderProps,
    TooltipTriggerProps,
};
